- name: "mo is downloaded and installed"
  get_url:
    url:  https://raw.githubusercontent.com/tests-always-included/mo/master/mo
    dest: /usr/local/bin/mo
    force: no
    mode: +x
  become: true

