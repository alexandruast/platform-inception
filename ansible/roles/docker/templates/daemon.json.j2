#jinja2: lstrip_blocks: "True"
{
  "dns": [
    {% if service_bind_ip is defined %}
    "{{service_bind_ip}}",
    {% else %}
    "{{ansible_default_ipv4.address}}",
    {% endif %}
    "8.8.8.8",
    "8.8.4.4"
  ],
  "hosts": [
    {% if docker_host is defined %}
    "{{docker_host}}"
    {% endif %}
  ]
}