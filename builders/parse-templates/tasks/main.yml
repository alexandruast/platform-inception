- template:
    src:  "{{ item.src }}"
    dest: "{{ (item.src|splitext)[0] }}"
  with_filetree: "{{ checkout_dir }}"
  register: parsed_templates
  when: item.state == 'file'
    and (item.src|splitext)[1] == '.j2'
  no_log: true